# In your FRONTEND directory (where vite.config.js is)
cd /path/to/your/frontend

# Create .env file
cat > .env << 'EOF'
VITE_API_URL=http://localhost:5000/api
EOF

# Or simply:
echo "VITE_API_URL=http://localhost:5000/api" > .env

# Verify
cat .env